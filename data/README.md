This folder contains shared A/B test scenario data and helpers.

- CSV files: moved from `verification/data/`.
- `data_generator.py`: generates synthetic scenarios used by verification tests.

The `verification/` package and demos read data from here.
